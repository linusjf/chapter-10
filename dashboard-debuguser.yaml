---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: runtime-debug-user
  namespace: kubernetes-dashboard

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: runtime-debug-clusterrole
rules:
  - apiGroups: ["", "apps", "batch", "extensions"]
    resources:
      [
        "pods",
        "pods/log",
        "pods/exec",
        "pods/portforward",
        "pods/ephemeralcontainers",
        "services",
        "endpoints",
        "deployments",
        "replicasets",
        "daemonsets",
        "statefulsets",
        "jobs",
        "cronjobs",
        "configmaps",
        "secrets",
        "namespaces",
        "nodes",
        "events",
        "ingresses",
        "ingressclasses"
      ]
    verbs: ["get", "list", "watch", "create"]
    # create is needed for exec/portforward/debug

  - apiGroups: ["metrics.k8s.io"]
    resources: ["pods", "nodes"]
    verbs: ["get", "list", "watch"]

  - apiGroups: ["authorization.k8s.io"]
    resources: ["selfsubjectaccessreviews", "selfsubjectrulesreviews"]
    verbs: ["create"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: runtime-debug-clusterrolebinding
subjects:
  - kind: ServiceAccount
    name: runtime-debug-user
    namespace: kubernetes-dashboard
roleRef:
  kind: ClusterRole
  name: runtime-debug-clusterrole
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: v1
kind: Secret
metadata:
  name: runtime-debug-user
  namespace: kubernetes-dashboard
  annotations:
    kubernetes.io/service-account.name: "runtime-debug-user"
type: kubernetes.io/service-account-token
